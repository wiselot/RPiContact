package org.wiselot.RPiContact.Handle;

import java.util.concurrent.ThreadPoolExecutor;

public class MessageHandle extends SimpleHandle{
    public MessageHandle(ThreadPoolExecutor pool) {
        super(pool);
    }
}
